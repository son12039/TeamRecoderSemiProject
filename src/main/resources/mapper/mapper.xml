<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.TestMapper">
 <insert id="register" parameterType="SignUp">
   INSERT INTO sign_up (id,pwd,name)
   VALUES (#{id},#{pwd},#{name})
 </insert>
 <select id="login" parameterType="SignUp" resultType="SignUp">
 SELECT *
 FROM sign_up
 WHERE id=#{id} AND pwd=#{pwd}
 </select>
 </mapper>