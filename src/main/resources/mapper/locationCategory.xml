<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.locationCategoryMapper">

	<resultMap id="Location" type="LocationCategory">
		<id column="loc_code" property="locCode"/>
		<result column="loc_la_name" property="locLaName"/>
		<result column="loc_s_name" property="locSName"/>
	</resultMap>
	
	
	<select id="allLocationLarge" resultMap="Location">
		SELECT loc_la_name FROM location_category
		group by loc_la_name;
	</select>
	
	<select id="allLocationSmall" parameterType="String" resultMap="Location">
		SELECT loc_s_name FROM location_category
		WHERE loc_la_name =#{locLaName};
	</select>
	
	
	
	
	<!-- 이거는 전체 보여주는거 -->
	<!--  옛 유산들
	<select id="allLocationLarge" resultMap="LocationCategory">
		SELECT * FROM LOCATION_CATEGORY_LARGE
	</select>

	<resultMap id="smallCategories" type="LocationCategorySmall">
			<id column="loc_s_code" property="locSmallCode"/>
			<result column="loc_s_name" property="locSmallName"/>
		</resultMap>

	<select id="allLocationSmall" parameterType="Integer" resultMap="smallCategories">
		SELECT loc_s_name FROM location_category_small
		WHERE loc_la_code = #{code}
	</select>
	 -->
</mapper>